AUTH_SECRET="" # chave longa e única
#DIRECT_URL="" # opcional (conexão direta)
POSTGRES_DB="" # ex: brunodb
POSTGRES_USER="" # ex: brunodb_owner
POSTGRES_PASSWORD="" # senha do banco
POSTGRES_HOST="db" # opcional (padrão: db)
POSTGRES_PORT="5432" # opcional (padrão: 5432)
POSTGRES_SSLMODE="disable" # opcional (padrão: disable)

# Seed inicial obrigatório (sem credenciais hardcoded)
SEED_MASTER_EMAIL=""
SEED_MASTER_PASSWORD=""
SEED_MASTER_NAME="Master"
SEED_MASTER_CPF=""
